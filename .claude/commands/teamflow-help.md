---
description: Print a digest of all agent-teamflow slash commands available in this install.
---

Read `CLAUDE.md`, then follow `skills/teamflow-help.md` exactly. This command does NOT require an `.agent-teamflow` — it works in any repo.

Arguments: $ARGUMENTS

---
description: Split a brain dump into parallel issues for multiple team members, write a workflow file, and push.
---

Read `CLAUDE.md`, then read `.agent-teamflow` from the repo root, then follow `skills/dispatch.md` exactly.

Arguments: $ARGUMENTS

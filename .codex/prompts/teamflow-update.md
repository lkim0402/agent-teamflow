---
description: Pull the latest agent-teamflow into the install dir and re-register slash commands.
---

Read `AGENTS.md`, then follow `skills/teamflow-update.md` exactly. This command does NOT require an `.agent-teamflow` — it updates the install itself.

Arguments: $ARGUMENTS

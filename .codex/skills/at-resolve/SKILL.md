---
name: at-resolve
description: Use when the user explicitly selects the at-resolve skill or wants to pick open issues assigned to them and implement them in parallel; each issue gets its own worktree and branch.
---

# at-resolve

Read `AGENTS.md`, then read `.agent-teamflow` from the repo root, then follow `skills/resolve.md` exactly.

Treat the user's remaining request text as `$ARGUMENTS`.

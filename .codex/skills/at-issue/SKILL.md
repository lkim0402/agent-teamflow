---
name: at-issue
description: Use when the user explicitly selects the at-issue skill or wants to create one or more branch-sized GitHub/GitLab issues from a brain dump; drafts, previews, then posts with confirmation.
---

# at-issue

Read `AGENTS.md`, then read `.agent-teamflow` from the repo root, then follow `skills/issue.md` exactly.

Treat the user's remaining request text as `$ARGUMENTS`.

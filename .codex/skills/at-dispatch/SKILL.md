---
name: at-dispatch
description: Use when the user explicitly selects the at-dispatch skill or wants to split a larger brain dump into parallel issues for multiple team members, write a workflow file, and push.
---

# at-dispatch

Read `AGENTS.md`, then read `.agent-teamflow` from the repo root, then follow `skills/dispatch.md` exactly.

Treat the user's remaining request text as `$ARGUMENTS`.

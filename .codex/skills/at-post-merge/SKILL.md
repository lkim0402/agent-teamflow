---
name: at-post-merge
description: Use when the user explicitly selects the at-post-merge skill or wants to label linked issues after merging an integration-to-staging MR/PR.
---

# at-post-merge

Read `AGENTS.md`, then read `.agent-teamflow` from the repo root, then follow `skills/post-merge.md` exactly.

Treat the user's remaining request text as `$ARGUMENTS`.

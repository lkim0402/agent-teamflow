---
name: at-prod-check
description: Use when the user explicitly selects the at-prod-check skill or wants a pre-production code review scoped to recent commits, checking impact, contracts, auth, stability, and regressions.
---

# at-prod-check

Read `AGENTS.md`, then read `.agent-teamflow` from the repo root, then follow `skills/prod-check.md` exactly.

Treat the user's remaining request text as `$ARGUMENTS`.

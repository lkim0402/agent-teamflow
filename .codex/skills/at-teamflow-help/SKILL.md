---
name: at-teamflow-help
description: Use when the user explicitly selects the at-teamflow-help skill or wants a digest of all agent-teamflow commands/workflows available in this install.
---

# at-teamflow-help

Read `AGENTS.md`, then follow `skills/teamflow-help.md` exactly.

This workflow does not require `.agent-teamflow`; it works in any repo. Treat the user's remaining request text as `$ARGUMENTS`.

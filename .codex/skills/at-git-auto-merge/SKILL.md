---
name: at-git-auto-merge
description: Use when the user explicitly selects the at-git-auto-merge skill or wants to commit, push a feature branch, merge into their integration branch, then open or update an MR/PR to staging.
---

# at-git-auto-merge

Read `AGENTS.md`, then read `.agent-teamflow` from the repo root, then follow `skills/git-auto-merge.md` exactly.

Treat the user's remaining request text as `$ARGUMENTS`.

---
name: at-teamflow-update
description: Use when the user explicitly selects the at-teamflow-update skill or wants to pull the latest agent-teamflow into the install directory and re-register runtime adapters.
---

# at-teamflow-update

Read `AGENTS.md`, then follow `skills/teamflow-update.md` exactly.

This workflow does not require `.agent-teamflow`; it updates the install itself. Treat the user's remaining request text as `$ARGUMENTS`.

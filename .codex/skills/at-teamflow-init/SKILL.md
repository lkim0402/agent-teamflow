---
name: at-teamflow-init
description: Use when the user explicitly selects the at-teamflow-init skill or wants to bootstrap the current repo for agent-teamflow by writing .agent-teamflow and optionally creating personal integration branches.
---

# at-teamflow-init

Read `AGENTS.md`, then follow `skills/teamflow-init.md` exactly.

This workflow does not require an existing `.agent-teamflow`; it writes one. Treat the user's remaining request text as `$ARGUMENTS`.
